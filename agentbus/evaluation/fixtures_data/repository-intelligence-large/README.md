# Deterministic Large Repository Seed

The repository-intelligence evaluator expands this owned fixture with bounded,
deterministic Python modules before indexing. No generated file leaves the
evaluation-owned temporary repository.
