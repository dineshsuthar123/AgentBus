# Example task

Add a `divide(left, right)` function to `calculator.py`. Raise `ValueError` for
division by zero, add focused pytest coverage, and document the behavior in the
README. Keep changes limited to this example repository.
